<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<title>Search Results - <%= query %></title>
		<link rel="stylesheet" href="/css/home.css" />
		<link rel="stylesheet" href="/css/sidebar.css" />
		<style>
			body {
				background: #0b0b0b;
				color: #fff;
				font-family: "Ubuntu", sans-serif;
				padding: 40px;
			}
			h1 {
				text-align: center;
				margin-bottom: 30px;
			}
			.results-grid {
				display: grid;
				grid-template-columns: repeat(auto-fill, minmax(220px, 1fr));
				gap: 20px;
				justify-items: center;
			}
			.result-card {
				background: #1b1b1b;
				border-radius: 10px;
				overflow: hidden;
				width: 220px;
				text-align: center;
				transition: 0.3s ease;
			}
			.result-card:hover {
				transform: scale(1.05);
				box-shadow: 0 4px 10px rgba(255, 255, 255, 0.2);
			}
			.result-card img {
				width: 100%;
				height: 280px;
				object-fit: cover;
			}
			.result-card h3 {
				padding: 10px;
				font-size: 1rem;
				color: #ffd700;
			}
			.result-card p {
				padding: 0 10px 10px;
				font-size: 0.85rem;
				color: #bbb;
			}
		</style>
	</head>
	<body>
		<h1>Search Results for "<%= query %>"</h1>

		<% if (results.length === 0) { %>
		<p style="text-align: center">No results found.</p>
		<% } else { %>
		<div class="results-grid">
			<% results.forEach(series => { %>
			<a href="/watchlist/series/<%= series.series_id %>" class="result-card">
				<img src="<%= series.poster_url %>" alt="<%= series.title %>" />
				<h3><%= series.title %></h3>
				<p>⭐ <%= series.series_rating %> | <%= series.genre_name %></p>
				<p>
					<%= series.summary.length > 100 ? series.summary.slice(0, 100) + '...'
					: series.summary %>
				</p>
			</a>
			<% }) %>
		</div>

		<% } %>
	</body>
</html>
